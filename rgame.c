#include<stdio.h>
#include
typedef long long ll;
ll res=0,b[100010];
int int main(int argc, char const *argv[])
{
	int t,n,i,j,a[100010];
	scanf("%d",&t);
	k=0;
	while(t--){
		scanf("%d",&n);
		for (int i = 0; i < n; ++i)
		{
			scanf("%d",&a[i]);
			for()
		}

	}
	return 0;
}